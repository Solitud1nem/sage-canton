# Daml
.daml/
*.dar
# ...but DO commit vendored CIP-0056 / Amulet dependency DARs so the repo builds standalone
!daml/vendor/
!daml/vendor/*.dar

# Codegen output
generated-ts/
generated-java/

# Node / frontend (added later)
node_modules/
dist/
.next/
*.log

# Env / secrets — never commit keys, party tokens, JWTs
.env
.env.*
*.jwt
secrets/

# OS / editor
.DS_Store
.vscode/
.idea/
