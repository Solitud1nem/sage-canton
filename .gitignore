# Daml
.daml/
*.dar
# ...but DO commit vendored CIP-0056 / Amulet dependency DARs so the repo builds standalone
!daml/vendor/
!daml/vendor/*.dar

# Codegen output
generated-ts/
generated-java/

# Node / frontend (added later)
node_modules/
dist/
.next/
*.log

# Env / secrets — never commit keys, party tokens, JWTs
.env
.env.*
# ...but DO commit the .env.example template (names only, no secrets)
!.env.example
!**/.env.example
*.jwt
secrets/

# OS / editor
.DS_Store
.vscode/
.idea/

# AI-assistant context & planning — keep local, never publish
CLAUDE.md
AGENTS.md
PLANNING.md
DECK-PLAN.md
